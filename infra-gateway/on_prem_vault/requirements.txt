# NEXUS infra-gateway requirements (Python 3.13 compatible)

# HTTP client (sync_service.py → Cloud Ingestor)
requests==2.31.0

# Kafka (Python-native, no C build issues)
aiokafka==0.10.0

# FastAPI + ASGI server (cloud-ingestor API)
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ✅ Updated for Python 3.13 compatibility
pydantic>=2.10,<3

# JSON schema validation
jsonschema==4.22.0

# Dev/testing only
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0